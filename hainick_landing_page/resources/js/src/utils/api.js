// Otomatis mendeteksi domain/IP tempat web sedang dibuka (lokal maupun Hostinger)
export const BASE_URL = window.location.origin;

// Prefix gabungan Laravel API + nama projek (sudah diperbaiki typo double 'k')
export const API_URL = `${BASE_URL}/api/hainickkreatif`;
